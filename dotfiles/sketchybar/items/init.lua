require("items.calendar")
-- sysstats right after calendar so it lands just left of the date/time
-- (right-positioned items added later are placed further to the left).
require("items.widgets.sysstats")
require("items.media")
require("items.spaces")
